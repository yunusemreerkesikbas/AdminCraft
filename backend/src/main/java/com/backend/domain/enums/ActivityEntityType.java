package com.backend.domain.enums;

/**
 * Enum representing the type of entity for activity tracking.
 */
public enum ActivityEntityType {
    PAGE,
    COMPONENT,
    MEDIA,
    PRODUCT,
    SITE,
    SITE_SETTINGS,
    NAVIGATION
}
