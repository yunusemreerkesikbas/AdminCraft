package com.backend.domain.enums;

/**
 * Output format for generated media variants.
 * Determines the image format of processed variants.
 */
public enum OutputFormat {
  ORIGINAL, // Keep original format
  JPEG,
  PNG,
  WEBP
}
