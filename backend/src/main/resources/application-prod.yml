# Production Profile - All sensitive values from environment variables

spring:
  datasource:
    platform:
      jdbc-url: ${PLATFORM_DB_URL}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
    tenant:
      host: ${DB_HOST}
      port: ${DB_PORT:3306}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        show_sql: false
        use_sql_comments: false

  flyway:
    url: ${PLATFORM_DB_URL}
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  mail:
    host: ${SMTP_HOST}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

craftive:
  storage:
    provider: s3
    s3:
      endpoint: https://fra1.digitaloceanspaces.com
      bucket: craftive-media-prod
      region: fra1
      cdn-base-url: https://media.craftive.io
      access-key: ${SPACES_ACCESS_KEY}
      secret-key: ${SPACES_SECRET_KEY}

app:
  jwt:
    secret: ${JWT_SECRET}
    cookie:
      secure: true
  frontend:
    base-url: ${APP_FRONTEND_BASE_URL:https://%s.craftive.io}
    admin-url: ${APP_FRONTEND_ADMIN_URL:https://app.craftive.io}
  tenant:
    auto-sync-on-startup: true
    trust-forwarded-host: ${TENANT_TRUST_FORWARDED_HOST:false}
  commerce:
    payment:
      callback-base-url: ${APP_COMMERCE_PAYMENT_CALLBACK_BASE_URL:https://%s.craftive.io/api}
    sms:
      provider: ${APP_COMMERCE_SMS_PROVIDER:disabled}
  platform-domain: ${PLATFORM_DOMAIN:craftive.io}
  email:
    from-address: ${EMAIL_FROM_ADDRESS:noreply@craftive.io}
    from-name: ${EMAIL_FROM_NAME:Craftive}
    log-content: false
  cors:
    allowed-origins:
      - http://localhost:4200
      - http://127.0.0.1:4200
      - http://admin.localhost:4200
      - https://app.craftive.io
      - https://craftive.io
      - https://www.craftive.io
    allowed-origin-patterns:
      - http://*.localhost:4200
      - https://*.craftive.io
  otp:
    bypass-code:
  encryption:
    secret-key: ${RECAPTCHA_MASTER_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
  health:
    mail:
      enabled: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    com.backend: INFO
    org.springframework.security: WARN
    org.hibernate: WARN
