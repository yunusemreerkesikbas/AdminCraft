Full Refactor: Tenant Response DTO + Auto-Provisioning
🎯 Hedefler
Response DTO Optimization: Gereksiz alanları kaldır, eksik alanları ekle
Auto-Provisioning Workflow: Tenant oluşturulduktan sonra otomatik provision
Admin Info Separation: Admin bilgileri ayrı endpoint'e taşı (security)
Provisioning Status Tracking: Response'ta provisioning durumu göster
📦 Backend Changes (6 files)

1. Create TenantListResponse.java (NEW)
Path: backend/.../dto/response/TenantListResponse.java Minimal DTO for list view:
record TenantListResponse(
    Long id,
    String subdomain,
    String companyName,
    TenantStatus status,
    String statusDisplay,
    Language defaultLanguage,
    String defaultLanguageDisplay,
    Set<Language> supportedLanguages,

    // Provisioning info (NEW)
    String provisioningStatus,  // idle | provisioning | failed
    Integer provisionedModulesCount,

    // Metadata
    LocalDateTime createdAt,
    LocalDateTime activatedAt,

    // Optional
    String customDomain,
    Boolean sslEnabled
)
2. Create TenantDetailResponse.java (NEW)
Path: backend/.../dto/response/TenantDetailResponse.java Full DTO extending TenantListResponse (composition pattern)
3. Create TenantAdminInfoResponse.java (NEW)
Path: backend/.../dto/response/TenantAdminInfoResponse.java
record TenantAdminInfoResponse(
    String adminEmail,
    String adminName,
    String phone,
    LocalDateTime lastLoginAt  // future
)
4. Update TenantController.java
Change GET /api/tenants → return List<TenantListResponse>
Change GET /api/tenants/{id} → return TenantDetailResponse
Add GET /api/tenants/{id}/admin → return TenantAdminInfoResponse (@PreAuthorize SUPER_ADMIN)
5. Update TenantServiceImpl.java
Add getTenantAdminInfo(Long id) method
Add provisioning status logic:
Query latest ProvisioningJob for tenant
Count enabled modules from tenant_modules
Return status: idle | provisioning | failed
6. Deprecate old TenantResponse.java
Add @Deprecated annotation
Keep for backward compatibility (1 sprint)
🎨 Frontend Changes (3 files)
7. Update tenants.types.ts
Add new interfaces:
interface TenantListResponse {
    id: number;
    subdomain: string;
    companyName: string;
    status: TenantStatus;
    statusDisplay: string;
    defaultLanguage: Language;
    defaultLanguageDisplay: string;
    supportedLanguages: Language[];

    // NEW fields
    provisioningStatus: 'idle' | 'provisioning' | 'failed';
    provisionedModulesCount: number;

    createdAt: string;
    activatedAt?: string;
    customDomain?: string;
    sslEnabled?: boolean;
}

interface TenantAdminInfo {
    adminEmail: string;
    adminName: string;
    phone?: string;
}
8. Update tenants-list.component.ts
Line 128-133 - Add auto-provision:
next: (tenant) => {
    this.store.addItem(tenant);
    this.#notify.success('Tenant oluşturuldu. Modül seçimi yapılıyor...');

    // ✅ Auto-open provision dialog
    this.provisionTenant(tenant);
}
Add admin info fetch method:
viewAdminInfo(tenantId: number): void {
    this.service.getAdminInfo(tenantId)
        .subscribe(info => {
            // Show in dialog
        });
}
9. Update tenants.service.ts
Add new method:
getAdminInfo(tenantId: number): Observable<TenantAdminInfo> {
    return this._http.get<ApiResponse<TenantAdminInfo>>(`/api/tenants/${tenantId}/admin`)
        .pipe(map(res => res.data));
}
🧪 Testing (2 scenarios)
10. Backend Test - TenantServiceImplTest
Test getTenantAdminInfo() returns correct data
Test GET /api/tenants/{id}/admin requires SUPER_ADMIN
Test provisioning status calculation
11. Frontend Integration Test
Full workflow:
Create tenant → 201 Created
Provision dialog auto-opens
Select modules → POST /provisioning/...
Poll job status → COMPLETED
Verify tenant list shows provisionedModulesCount
🔒 Security Notes
Admin endpoint SUPER_ADMIN only
No PII (adminEmail, phone) in list response
Audit log for admin info access (future)
📊 Migration Strategy
Phase 1 (Sprint 17): Backend DTO refactor
Phase 2 (Sprint 17): Frontend auto-provision
Phase 3 (Sprint 18): Remove deprecated TenantResponse
✅ Acceptance Criteria
 GET /api/tenants returns TenantListResponse (no admin fields)
 GET /api/tenants/{id}/admin returns admin info (SUPER_ADMIN only)
 Tenant create → auto-opens provision dialog
 Provision dialog → starts job → polls status
 List view shows "3 modules provisioned" badge
 No breaking changes (backward compatible 1 sprint)
 Unit tests pass (backend + frontend)
Estimated Effort: 6-8 hours
Files Changed: 11 files (6 backend, 3 frontend, 2 tests)
