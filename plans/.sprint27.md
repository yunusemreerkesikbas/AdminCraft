# Sprint 27: Page Template System

## 🎯 Goal

SAP Commerce Cloud-style Page Templates enabling reusable page layouts with predefined slot structures.

---

## ✅ Decisions

| Decision           | Choice      | Notes                                        |
| ------------------ | ----------- | -------------------------------------------- |
| Template naming    | Descriptive | `homepage-template`, `content-page-template` |
| Slot restrictions  | Optional    | `allowed_types` JSON field, NULL = all       |
| Auto slot creation | ✅ Yes      | Assign template → auto-create page_slots     |
| System templates   | ✅ Yes      | Pre-seeded, cannot be deleted                |

---

## 📦 Database Migration

### V15\_\_page_templates.sql

```sql
CREATE TABLE page_templates (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(500) NULL,
    is_system BOOLEAN DEFAULT FALSE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT NULL,
    updated_by BIGINT NULL,

    UNIQUE KEY uk_page_template_uid (uid),
    INDEX idx_page_template_active (is_active)
);

CREATE TABLE template_slots (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,
    template_id BIGINT NOT NULL,
    slot_name VARCHAR(50) NOT NULL,
    position VARCHAR(20) NOT NULL,
    sort_order INT DEFAULT 0,
    is_required BOOLEAN DEFAULT FALSE,
    max_components INT NULL,
    allowed_types JSON NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT NULL,
    updated_by BIGINT NULL,

    UNIQUE KEY uk_template_slot (template_id, slot_name),
    FOREIGN KEY (template_id) REFERENCES page_templates(id) ON DELETE CASCADE
);

-- Add template_id to pages
ALTER TABLE pages
ADD COLUMN template_id BIGINT NULL AFTER category_id,
ADD INDEX idx_page_template (template_id),
ADD CONSTRAINT fk_page_template
    FOREIGN KEY (template_id) REFERENCES page_templates(id) ON DELETE SET NULL;
```

---

## 🏗️ Backend Files

| File                          | Description              |
| ----------------------------- | ------------------------ |
| `PageTemplate.java`           | Domain entity            |
| `TemplateSlot.java`           | Slot entity              |
| `Page.java`                   | Add `templateId`         |
| `PageTemplateRepository.java` | Repository               |
| `PageTemplateService.java`    | Service interface + impl |
| `PageTemplateController.java` | Admin REST endpoints     |

---

## 🔌 Admin API Endpoints

| Endpoint                                    | Method | Description         |
| ------------------------------------------- | ------ | ------------------- |
| `/api/page-templates`                       | GET    | List templates      |
| `/api/page-templates`                       | POST   | Create template     |
| `/api/page-templates/{id}`                  | GET    | Get with slots      |
| `/api/page-templates/{id}`                  | PUT    | Update template     |
| `/api/page-templates/{id}`                  | DELETE | Delete (non-system) |
| `/api/page-templates/{id}/slots`            | POST   | Add slot            |
| `/api/page-templates/{id}/slots/{slotName}` | DELETE | Remove slot         |
| `/api/page-templates/active`                | GET    | List active         |
| `/api/page-templates/{id}/assign/{pageId}`  | POST   | Assign to page      |

---

## 🎨 Frontend Files

| File                                     | Location                     | Description    |
| ---------------------------------------- | ---------------------------- | -------------- |
| `page-template.service.ts`               | `admin/page-templates/`      | Service        |
| `page-template.types.ts`                 | `admin/page-templates/`      | Types          |
| `page-template-list.component.ts`        | `admin/page-templates/list/` | List component |
| `page-template-edit-dialog.component.ts` | `admin/page-templates/`      | Edit dialog    |

### Endpoints

```typescript
pageTemplates: 'page-templates',
pageTemplatesActive: 'page-templates/active',
pageTemplateById: 'page-templates/${id}',
pageTemplateSlots: 'page-templates/${id}/slots',
pageTemplateSlot: 'page-templates/${id}/slots/${slotName}',
pageTemplateAssign: 'page-templates/${id}/assign/${pageId}'
```

---

## 🧭 Frontend Quick Guide (Admin)

- **List**: `GET /api/page-templates` returns templates with their slots.
- **Create**:
  - `POST /api/page-templates` creates a template.
  - Add slots by calling `POST /api/page-templates/{id}/slots` per slot.
- **Edit**: `PUT /api/page-templates/{id}` updates `name`, `description`, `isActive`.
- **Delete**: `DELETE /api/page-templates/{id}` (system templates are protected by backend).
- **Assign**: `POST /api/page-templates/{id}/assign/{pageId}` assigns a template and auto-creates missing `page_slots`.
- **Notes**:
  - `previewImage` is removed.
  - `allowedTypes`: `null` means “allow all types”, `[]` means “allow none”.

### Example responses (ApiResponse)

**GET `/api/page-templates`**

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "uuid": "3f6d2a7e-6b5f-4d5c-a94f-0d3d9f8c7b1a",
      "uid": "homepage-template",
      "name": "Homepage Template",
      "description": "Standard homepage layout",
      "isSystem": true,
      "isActive": true,
      "slots": [
        {
          "id": 10,
          "uuid": "7c1b2e3d-4f5a-678b-9c0d-1e2f3a4b5c6d",
          "slotName": "Header",
          "position": "TOP",
          "sortOrder": 0,
          "isRequired": true,
          "maxComponents": null,
          "allowedTypes": null
        }
      ]
    }
  ]
}
```

**POST `/api/page-templates`**

```json
{
  "success": true,
  "message": "Template created successfully",
  "data": {
    "id": 15,
    "uuid": "f0b1c2d3-4e5f-6789-a0b1-c2d3e4f56789",
    "uid": "landing-page-template",
    "name": "Landing Page Template",
    "description": "Marketing landing layout",
    "isSystem": false,
    "isActive": true,
    "slots": []
  }
}
```

**POST `/api/page-templates/{id}/slots`**

```json
{
  "success": true,
  "message": "Slot added successfully",
  "data": {
    "id": 99,
    "uuid": "11111111-2222-3333-4444-555555555555",
    "slotName": "HeroSection",
    "position": "TOP",
    "sortOrder": 1,
    "isRequired": false,
    "maxComponents": 1,
    "allowedTypes": ["HeroBanner"]
  }
}
```

**POST `/api/page-templates/{id}/assign/{pageId}`**

```json
{
  "success": true,
  "message": "Template assigned to page successfully",
  "data": null
}
```

---

## 🧪 Verification

```bash
cd backend && mvn test -Dtest=PageTemplateControllerIntegrationTest
```

### Tests

1. `createTemplate_ShouldReturnCreatedTemplate()`
2. `getTemplate_ShouldReturnWithSlots()`
3. `addSlot_ShouldAddToTemplate()`
4. `assignTemplateToPage_ShouldCreatePageSlots()`
5. `deleteSystemTemplate_ShouldFail()`

---

## 📊 Status

- [ ] V15\_\_page_templates.sql migration
- [ ] R\_\_seed_page_templates.sql seed data
- [ ] PageTemplate entity
- [ ] TemplateSlot entity
- [ ] Update Page entity
- [ ] Repository layer
- [ ] Service layer
- [ ] PageTemplateController
- [ ] Integration tests
- [ ] Frontend service + types
- [ ] Frontend UI components
