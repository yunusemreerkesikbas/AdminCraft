# Sprint 34: Multi-Language Enhancement

## Goal

Add composite create endpoints, enhanced fallback, and multi-language support to AdminCraft.

---

## Modules

1. **Pages** - composite create + fallback
2. **Components** - composite create + fallback
3. **Component Entries** - composite create + fallback
4. **Navigation** - add i18n support (NEW)
5. **Page Templates** - add i18n support (NEW)

---

## Phase 1: Database Migrations

### V18: Navigation i18n

**File**: `backend/src/main/resources/db/tenant/core/V18__navigation_i18n.sql`

```sql
CREATE TABLE navigation_node_i18n ();

CREATE TABLE navigation_entry_i18n ();

-- Migrate existing data to TR


-- Drop old columns
```

### V19: Page Template i18n

**File**: `backend/src/main/resources/db/tenant/pagebuilder/V19__page_template_i18n.sql`

```sql
CREATE TABLE page_template_i18n ();

-- Migrate existing data

-- Drop old columns
ALTER TABLE page_templates DROP COLUMN name, DROP COLUMN description;
```

---

## Phase 2: Backend - Entities

### Create New Entities

**Location**: `backend/src/main/java/com/backend/domain/entity/`

1. `NavigationNodeI18n.java` - extends BaseI18nEntity
2. `NavigationEntryI18n.java` - extends BaseI18nEntity
3. `PageTemplateI18n.java` - extends BaseI18nEntity

### Update Existing Entities

1. `NavigationNode.java` - remove `title`, add `List<NavigationNodeI18n> i18nContent`
2. `NavigationEntry.java` - remove `linkName`, add `List<NavigationEntryI18n> i18nContent`
3. `PageTemplate.java` - remove `name`/`description`, add `List<PageTemplateI18n> i18nContent`

---

## Phase 3: Backend - Repositories

### Create

**Location**: `backend/src/main/java/com/backend/domain/repository/`

1. `NavigationNodeI18nRepository.java`
2. `NavigationEntryI18nRepository.java`
3. `PageTemplateI18nRepository.java`

**Required Methods**:

```java
Optional<*I18n> findBy[Entity]IdAndLanguage(Long id, Language language);
List<*I18n> findBy[Entity]Id(Long id);
```

---

## Phase 4: Backend - Composite DTOs

### Create Request DTOs

**Location**: `backend/src/main/java/com/backend/application/dto/request/`

```java
// 1. PageCreateCompositeRequest.java
public record PageCreateCompositeRequest() {}

// 2. ComponentCreateCompositeRequest.java
public record ComponentCreateCompositeRequest() {}

// 3. ComponentEntryCreateCompositeRequest.java
public record ComponentEntryCreateCompositeRequest() {}

// 4. NavigationNodeCreateCompositeRequest.java
public record NavigationNodeCreateCompositeRequest() {}

// 5. NavigationEntryCreateCompositeRequest.java
public record NavigationEntryCreateCompositeRequest() {}

// 6. PageTemplateCreateCompositeRequest.java
public record PageTemplateCreateCompositeRequest() {}
```

---

## Phase 5: Backend - Services

### Add Composite Create Methods

**Update each service**:

```java
// PageService.java
@Transactional
PageDetailResponse createPageComposite(PageCreateCompositeRequest request, Long userId);

// ComponentService.java
@Transactional
ComponentDetailResponse createComponentComposite(ComponentCreateCompositeRequest request, Long userId);

// ComponentEntryService.java
@Transactional
ComponentEntryDetailResponse createEntryComposite(ComponentEntryCreateCompositeRequest request, Long userId);

// NavigationService.java
@Transactional
NavigationNodeResponse createNodeComposite(NavigationNodeCreateCompositeRequest request, Long userId);

@Transactional
NavigationEntryResponse createEntryComposite(NavigationEntryCreateCompositeRequest request, Long userId);

// PageTemplateService.java
@Transactional
PageTemplateDetailResponse createTemplateComposite(PageTemplateCreateCompositeRequest request, Long userId);
```

### Enhance Fallback Logic

**Update** `PageI18nServiceImpl.java`, `ComponentI18nServiceImpl.java`, `ComponentEntryI18nServiceImpl.java`:

```java
private *I18nResponse getFallbackLanguage*I18n(Long entityId) {
    // 1. Get tenant default language
    // 2. Try tenant default
    // 3. Fallback to first available
}
```

---

## Phase 6: Backend - Controllers

### Add Composite Endpoints

```java
// PageController.java
@PostMapping("/composite")
PageDetailResponse createComposite(@Valid @RequestBody PageCreateCompositeRequest request);

// ComponentController.java
@PostMapping("/composite")
ComponentDetailResponse createComposite(@Valid @RequestBody ComponentCreateCompositeRequest request);

// ComponentEntryController.java
@PostMapping("/composite")
ComponentEntryDetailResponse createComposite(@Valid @RequestBody ComponentEntryCreateCompositeRequest request);

// NavigationController.java
@PostMapping("/nodes/composite")
NavigationNodeResponse createNodeComposite(@Valid @RequestBody NavigationNodeCreateCompositeRequest request);

@PostMapping("/entries/composite")
NavigationEntryResponse createEntryComposite(@Valid @RequestBody NavigationEntryCreateCompositeRequest request);

// PageTemplateController.java
@PostMapping("/composite")
PageTemplateDetailResponse createComposite(@Valid @RequestBody PageTemplateCreateCompositeRequest request);
```

### DELETE Old Endpoints

**Remove**:

- `PUT /pages/{id}/i18n/{language}`
- `PUT /components/{id}/i18n/{language}`
- `PUT /entries/{id}/i18n/{language}`

**Keep** (read-only):

- `GET /pages/{id}/i18n/{language}`
- `GET /components/{id}/i18n/{language}`
- `GET /entries/{id}/i18n/{language}`

---

## Phase 7: Frontend - Services

### Update CrudHttpService

**File**: `storefront/src/app/core/crud/crud-http.service.ts`

```typescript
createComposite<CompositeDto>(dto: CompositeDto): Observable<T> {

}
```

### Update API Endpoints

**File**: `storefront/src/app/core/api/api-endpoints.ts`

```typescript
// Add composite endpoints
pagesComposite: 'pages/composite',
componentsComposite: 'components/composite',
componentEntriesComposite: 'components/entries/composite',
navigationNodesComposite: 'navigation/nodes/composite',
navigationEntriesComposite: 'navigation/entries/composite',
pageTemplatesComposite: 'page-templates/composite',
```

---

## Phase 8: Frontend - Forms

### Update Form Components

**Pattern** (example: `component-entry-form.component.ts`):

**OLD** (remove):

```typescript
#createEntry(): void {

}

#saveI18nData(entryId: number): void {  // REMOVE
  forkJoin([...i18nRequests]).subscribe(...);
}
```

**NEW**:

```typescript
#createEntry(): void {}

#buildTranslationsMap(): Record<Language, I18nData> {}
```

### Apply Same Pattern To:

- `page-form.component.ts`
- `component-form.component.ts`
- `navigation-node-dialog.component.ts`
- `navigation-entry-dialog.component.ts`
- `page-template-dialog.component.ts`

## Success Criteria

✅ Single request creates base + all translations
✅ Old endpoints deleted
✅ Fallback uses tenant default language
✅ Navigation supports multi-language
✅ Page Templates support multi-language
✅ Frontend simplified (no forkJoin)
✅ All tests pass

---
