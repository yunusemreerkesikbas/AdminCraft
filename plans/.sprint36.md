# Media Module Implementation Plan (Sprint 36)

## Executive Summary

SAP Commerce Cloud Hybris Media modülünün analizi ve AdminCraft'a adaptasyonu için kapsamlı plan.

---

## 1. SAP Hybris Media Architecture Analysis

### 1.1 Core Data Model

| Model                   | Purpose            | Key Fields                                                   |
| ----------------------- | ------------------ | ------------------------------------------------------------ |
| **MediaModel**          | Base file storage  | code, mimeType, size, altText (i18n), folder, catalogVersion |
| **MediaContainerModel** | Format grouping    | code, masterMedia, medias[]                                  |
| **MediaFormatModel**    | Processed versions | code, width, height, format                                  |
| **MediaFolder**         | Organization       | code, path, parent                                           |

### 1.2 Key Hybris Features

1. **Catalog-Aware Storage**: Media linked to product catalog versions
2. **Automatic Format Generation**: Single upload → thumbnail, detail, zoom, mobile
3. **Folder Hierarchy**: `/products/`, `/cms/`, `/marketing/`
4. **CDN Integration**: Immutable URLs, cache-friendly paths
5. **i18n Metadata**: altText, title, description per language
6. **DAM Principles**: Searchable, versioned, organized

### 1.3 Multi-Tenant Pattern

```
File Path: /media/{tenant_id}/{catalog_id}/{folder}/{media_code}.{ext}
Database: No tenant_id column (physical isolation via database-per-tenant)
```

### 1.4 Hybris Storage Strategies

| Strategy                           | Ortam      | Açıklama             |
| ---------------------------------- | ---------- | -------------------- |
| `LocalFileMediaStorageStrategy`    | On-premise | Yerel dosya sistemi  |
| `WindowsAzureBlobStorageStrategy`  | Cloud      | Azure Blob Storage   |
| `S3MediaStorageStrategy`           | Cloud      | Amazon S3            |
| `GoogleMediaStorageStrategy`       | Cloud      | Google Cloud Storage |
| `MongoDBGridFSMediaStorageFactory` | Hybrid     | MongoDB GridFS       |

> **Not**: AdminCraft Phase 1'de `LocalStorageAdapter`, Phase 2'de S3/Azure desteği planlanmıştır.

---

## 2. Current AdminCraft Media Module Status

### 2.1 What EXISTS

| Component        | Status       | Location                                         |
| ---------------- | ------------ | ------------------------------------------------ |
| ModuleCode Enum  | ✅ DONE      | `domain/enums/ModuleCode.java` (MEDIA entry)     |
| Platform Seed    | ✅ DONE      | `R__seed_modules.sql` (media registered)         |
| MediaFile Entity | ⚠️ PARTIAL   | `domain/entity/MediaFile.java`                   |
| MediaService     | ⚠️ STUB      | `application/service/MediaServiceImpl.java`      |
| MediaRepository  | ⚠️ INTERFACE | `domain/repository/MediaRepository.java`         |
| Frontend Module  | ⚠️ PARTIAL   | `storefront/src/app/modules/admin/custom/media/` |

### 2.2 What's MISSING (Critical)

| Component              | Status        | Action Needed                                   |
| ---------------------- | ------------- | ----------------------------------------------- |
| **Database Migration** | ❌ MISSING    | Create `db/tenant/media/V6__media_baseline.sql` |
| Backend Controllers    | ❌ MISSING    | Create `MediaController.java`                   |
| Upload Endpoint        | ❌ MISSING    | Create `/api/media/upload`                      |
| Frontend Components    | ❌ INCOMPLETE | Create list, upload, gallery views              |
| Navigation Entry       | ❓ CHECK      | Verify in `navigation-modules.constants.ts`     |

### 2.3 Current MediaFile Entity Analysis

```java
// Current structure (platform-level, NOT tenant-specific)
public class MediaFile {
    // Identifiers
    private Long id;
    private String uuid;  // No uid pattern like Component Library

    // File properties
    private String fileName, filePath, mimeType;
    private Long fileSize;
    private Integer width, height;

    // i18n (HARDCODED per language - not flexible)
    private String altTextTr, altTextEn;
    private String descriptionTr, descriptionEn;
    private String titleTr, titleEn;

    // Organization
    private String folder, category, tags (JSON);

    // Storage
    private String storageProvider; // local, s3, cloudinary
    private String externalUrl, externalId;

    // Metadata
    private String metadata (JSON);
    private Boolean hasThumbnails, isOptimized, isPublic;
}
```

**Issues Identified:**

1. Platform-level entity (should be tenant-specific for multi-tenant)
2. Hardcoded language columns (TR/EN) - not flexible
3. No BaseEntity extension (inconsistent with other modules)
4. No uuid/uid pattern (Component Library uses both)

---

## 3. SAP Features Applicability Analysis

### 3.1 RECOMMENDED Features (Implement)

| Feature                  | Priority | Rationale                             |
| ------------------------ | -------- | ------------------------------------- |
| **Folder Organization**  | HIGH     | Essential for DAM                     |
| **i18n Metadata**        | HIGH     | Consistent with other modules         |
| **Format Generation**    | MEDIUM   | Thumbnails essential, others optional |
| **MIME Type Validation** | HIGH     | Security                              |
| **File Size Limits**     | HIGH     | Resource management                   |
| **Search by Folder/Tag** | MEDIUM   | UX improvement                        |

### 3.2 OPTIONAL Features (Phase 2)

| Feature              | Priority | Rationale                        |
| -------------------- | -------- | -------------------------------- |
| CDN Integration      | LOW      | Configurable later               |
| Video Transcoding    | LOW      | Complex, external service needed |
| Format Definition UI | LOW      | Can use defaults                 |

> [!IMPORTANT] > **MediaContainer**: Başlangıçta "overkill" olarak değerlendirilmişti, ancak analiz sonrası
> master + variants gruplama CMS için kritik bulunmuş ve Phase 1'e alınmıştır (Section 4'te mevcut).

### 3.3 NOT RECOMMENDED Features (Skip)

| Feature                 | Reason                                   |
| ----------------------- | ---------------------------------------- |
| Catalog Version Linkage | AdminCraft doesn't have product catalogs |
| Complex DAM Workflows   | Out of scope for CMS                     |
| Asset Versioning        | Overkill for current needs               |

---

## 4. Recommended Architecture

### 4.1 Database Schema (Tenant-Specific - Full DAM)

```sql
-- V6__media_baseline.sql

-- ============================================
-- 1. MEDIA FORMAT DEFINITIONS (System + Custom)
-- ============================================
CREATE TABLE media_formats (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,
    code VARCHAR(50) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    width INT NOT NULL,
    height INT NOT NULL,
    quality INT DEFAULT 80,
    crop_mode ENUM('FIT', 'FILL', 'COVER') DEFAULT 'FIT',
    is_system BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_media_format_code (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- System format presets (R__seed_media_formats.sql)
-- THUMBNAIL: 100x100, SMALL: 300x300, MEDIUM: 600x600, LARGE: 1200x1200

-- ============================================
-- 2. MEDIA FILES (Master/Original)
-- ============================================
CREATE TABLE media (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,

    -- File Properties
    original_name VARCHAR(255) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_path VARCHAR(500) NOT NULL,
    mime_type VARCHAR(100) NOT NULL,
    file_extension VARCHAR(20),
    file_size BIGINT NOT NULL,

    -- Image/Video Properties
    width INT,
    height INT,
    duration INT,  -- video duration in seconds

    -- Organization (SAP Hybris style)
    folder_id BIGINT,
    tags JSON,

    -- Access Control
    is_public BOOLEAN DEFAULT TRUE,
    uploaded_by BIGINT,

    -- Storage Provider
    storage_provider ENUM('LOCAL', 'S3', 'AZURE', 'CLOUDINARY') DEFAULT 'LOCAL',
    external_url VARCHAR(1000),
    external_id VARCHAR(255),

    -- Metadata (EXIF, etc.)
    metadata JSON,
    usage_count INT DEFAULT 0,

    -- Status
    status ENUM('PROCESSING', 'ACTIVE', 'ARCHIVED', 'FAILED') DEFAULT 'PROCESSING',

    -- Audit
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT,
    updated_by BIGINT,
    last_accessed_at TIMESTAMP,

    INDEX idx_media_folder (folder_id),
    INDEX idx_media_mime_type (mime_type),
    INDEX idx_media_status (status),
    INDEX idx_media_is_public (is_public),
    INDEX idx_media_uploaded_by (uploaded_by)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================
-- 3. MEDIA I18N (Localized Metadata)
-- ============================================
CREATE TABLE media_i18n (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,
    media_id BIGINT NOT NULL,
    language ENUM('TR', 'EN') NOT NULL,

    alt_text VARCHAR(500),
    title VARCHAR(255),
    description TEXT,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    UNIQUE KEY uk_media_i18n_lang (media_id, language),
    FOREIGN KEY (media_id) REFERENCES media(id) ON DELETE CASCADE,
    INDEX idx_media_i18n_media_id (media_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================
-- 4. MEDIA CONTAINERS (SAP Hybris Pattern)
-- Groups master + generated formats together
-- ============================================
CREATE TABLE media_containers (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,
    code VARCHAR(100) NOT NULL UNIQUE,

    master_media_id BIGINT NOT NULL,  -- Original upload

    -- Audit
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT,
    updated_by BIGINT,

    FOREIGN KEY (master_media_id) REFERENCES media(id) ON DELETE CASCADE,
    INDEX idx_container_master (master_media_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================
-- 5. MEDIA CONTAINER ITEMS (Format Variants)
-- Links container to generated format versions
-- ============================================
CREATE TABLE media_container_items (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    container_id BIGINT NOT NULL,
    format_id BIGINT NOT NULL,
    media_id BIGINT NOT NULL,  -- Generated version

    FOREIGN KEY (container_id) REFERENCES media_containers(id) ON DELETE CASCADE,
    FOREIGN KEY (format_id) REFERENCES media_formats(id) ON DELETE RESTRICT,
    FOREIGN KEY (media_id) REFERENCES media(id) ON DELETE CASCADE,

    UNIQUE KEY uk_container_format (container_id, format_id),
    INDEX idx_container_item_container (container_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================
-- 6. MEDIA FOLDERS (Hierarchical Organization)
-- ============================================
CREATE TABLE media_folders (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    uid VARCHAR(50) NOT NULL UNIQUE,
    code VARCHAR(100) NOT NULL,
    name VARCHAR(255) NOT NULL,

    parent_id BIGINT,
    path VARCHAR(1000) NOT NULL,  -- Full path: /products/shoes/
    depth INT DEFAULT 0,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT,
    updated_by BIGINT,

    UNIQUE KEY uk_folder_path (path),
    FOREIGN KEY (parent_id) REFERENCES media_folders(id) ON DELETE CASCADE,
    INDEX idx_folder_parent (parent_id),
    INDEX idx_folder_depth (depth)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Add FK for media.folder_id
ALTER TABLE media
ADD CONSTRAINT fk_media_folder
FOREIGN KEY (folder_id) REFERENCES media_folders(id) ON DELETE SET NULL;
```

### 4.2 Seed Data (R\_\_seed_media_formats.sql)

```sql
-- System format presets (SAP Hybris style)
INSERT INTO media_formats (uuid, uid, code, name, width, height, quality, crop_mode, is_system)
VALUES
    (UUID(), 'mediaformat_thumb', 'THUMBNAIL', 'Thumbnail', 100, 100, 80, 'COVER', TRUE),
    (UUID(), 'mediaformat_small', 'SMALL', 'Small', 300, 300, 85, 'FIT', TRUE),
    (UUID(), 'mediaformat_medium', 'MEDIUM', 'Medium', 600, 600, 85, 'FIT', TRUE),
    (UUID(), 'mediaformat_large', 'LARGE', 'Large', 1200, 1200, 90, 'FIT', TRUE),
    (UUID(), 'mediaformat_zoom', 'ZOOM', 'Zoom', 2000, 2000, 95, 'FIT', TRUE)
ON DUPLICATE KEY UPDATE name = VALUES(name);

-- Root folder
INSERT INTO media_folders (uuid, uid, code, name, path, depth)
VALUES (UUID(), 'mediafolder_root', 'root', 'Root', '/', 0)
ON DUPLICATE KEY UPDATE name = VALUES(name);
```

### 4.3 Backend Structure (Full DAM)

```
backend/src/main/java/com/backend/
├── domain/
│   ├── entity/
│   │   ├── Media.java              (NEW - extends BaseEntity)
│   │   ├── MediaI18n.java          (NEW - extends BaseI18nEntity)
│   │   ├── MediaFormat.java        (NEW - format definitions)
│   │   ├── MediaContainer.java     (NEW - groups master + variants)
│   │   ├── MediaContainerItem.java (NEW - container-format link)
│   │   └── MediaFolder.java        (NEW - hierarchical folders)
│   ├── repository/
│   │   ├── MediaRepository.java    (REFACTOR interface)
│   │   ├── MediaI18nRepository.java (NEW)
│   │   ├── MediaFormatRepository.java (NEW)
│   │   ├── MediaContainerRepository.java (NEW)
│   │   └── MediaFolderRepository.java (NEW)
│   └── enums/
│       ├── StorageProvider.java    (NEW: LOCAL, S3, AZURE, CLOUDINARY)
│       ├── MediaStatus.java        (NEW: PROCESSING, ACTIVE, ARCHIVED, FAILED)
│       └── CropMode.java           (NEW: FIT, FILL, COVER)
├── application/
│   ├── service/
│       ├── MediaService.java       (REFACTOR - interface)
│       ├── MediaServiceImpl.java   (REFACTOR - implementation)
│       ├── MediaStorageService.java (NEW - file I/O)
│       ├── MediaProcessingService.java (NEW - resize/format generation)
│       ├── MediaContainerService.java (NEW)
│       ├── MediaFolderService.java (NEW)
│       ├── MediaFormatService.java (NEW)
│       └── MediaI18nService.java   (NEW)
├── infrastructure/
│   ├── persistence/repository/
│   │   ├── MediaRepositoryImpl.java
│   │   ├── MediaFormatRepositoryImpl.java
│   │   ├── MediaContainerRepositoryImpl.java
│   │   └── MediaFolderRepositoryImpl.java
│   └── storage/
│       ├── StorageAdapter.java     (NEW - interface)
│       ├── LocalStorageAdapter.java (NEW)
│       └── S3StorageAdapter.java   (FUTURE)
└── presentation/
    ├── controller/
    │   ├── MediaController.java    (NEW - CRUD + upload)
    │   ├── MediaContainerController.java (NEW)
    │   └── MediaFolderController.java (NEW)
    └── dto/
        ├── request/
        │   ├── MediaUploadRequest.java
        │   ├── MediaUpdateRequest.java
        │   ├── MediaI18nRequest.java
        │   ├── MediaContainerRequest.java
        │   └── MediaFolderRequest.java
        └── response/
            ├── MediaResponse.java
            ├── MediaDetailResponse.java (with i18n + container)
            ├── MediaContainerResponse.java
            ├── MediaFormatResponse.java
            └── MediaFolderResponse.java
```

### 4.4 API Endpoints (Full DAM)

```http
# ==========================================
# MEDIA (Core CRUD)
# ==========================================
GET    /api/media                  # List (paginated, filterable)
POST   /api/media/upload           # Upload file (multipart/form-data)
GET    /api/media/{id}             # Get by ID
GET    /api/media/{id}?include=container  # Get with container info
PUT    /api/media/{id}             # Update metadata
DELETE /api/media/{id}             # Delete
GET    /api/media/by-uid/{uid}     # Get by UID

# i18n
GET    /api/media/{id}/i18n/{lang} # Get i18n
PUT    /api/media/{id}/i18n/{lang} # Update i18n

# Composite (atomic create with translations)
POST   /api/media/composite        # Upload + i18n in one request

# ==========================================
# MEDIA CONTAINERS (SAP Hybris Pattern)
# ==========================================
GET    /api/media/containers                    # List containers
POST   /api/media/containers                    # Create container (auto from upload)
GET    /api/media/containers/{id}               # Get container with all formats
POST   /api/media/containers/{id}/generate      # Trigger format generation
DELETE /api/media/containers/{id}               # Delete container + all media

# ==========================================
# MEDIA FORMATS (Definitions)
# ==========================================
GET    /api/media/formats                       # List formats
POST   /api/media/formats                       # Create custom format
GET    /api/media/formats/{id}                  # Get format
PUT    /api/media/formats/{id}                  # Update format
DELETE /api/media/formats/{id}                  # Delete (non-system only)

# ==========================================
# MEDIA FOLDERS (Organization)
# ==========================================
GET    /api/media/folders                       # List all folders (tree)
POST   /api/media/folders                       # Create folder
GET    /api/media/folders/{id}                  # Get folder with children
PUT    /api/media/folders/{id}                  # Update folder
DELETE /api/media/folders/{id}                  # Delete folder (if empty)
PUT    /api/media/folders/{id}/move             # Move folder
GET    /api/media/folders/{id}/media            # Get media in folder

# ==========================================
# PUBLIC CMS DELIVERY
# ==========================================
GET    /api/cms/media/{uid}                     # Public access by UID
GET    /api/cms/media/{uid}?format=THUMBNAIL    # Get specific format
GET    /api/cms/media?uids=uid1,uid2            # Batch (max 50)
```

### 4.5 Frontend Structure (Full DAM)

```
storefront/src/app/modules/admin/custom/media/
├── services/
│   ├── media.service.ts           (REFACTOR - extends CrudHttpService)
│   ├── media.store.ts             (NEW - extends CrudStore)
│   ├── media-container.service.ts (NEW)
│   ├── media-folder.service.ts    (NEW)
│   └── media-format.service.ts    (NEW)
├── models/
│   └── media.types.ts             (REFACTOR - full type definitions)
│       ├── MediaDto
│       ├── MediaDetailDto
│       ├── MediaI18nDto
│       ├── MediaContainerDto
│       ├── MediaFormatDto
│       └── MediaFolderDto
├── components/
│   ├── media-list/                (NEW - grid/list view with filters)
│   │   ├── media-list.component.ts
│   │   └── media-list.component.html
│   ├── media-upload-dialog/       (NEW - drag & drop upload)
│   │   ├── media-upload-dialog.component.ts
│   │   └── media-upload-dialog.component.html
│   ├── media-detail-dialog/       (NEW - edit + i18n tabs)
│   │   ├── media-detail-dialog.component.ts
│   │   └── media-detail-dialog.component.html
│   ├── media-gallery/             (NEW - visual grid view)
│   │   ├── media-gallery.component.ts
│   │   └── media-gallery.component.html
│   ├── media-picker-dialog/       (NEW - reusable picker for other modules)
│   │   ├── media-picker-dialog.component.ts
│   │   └── media-picker-dialog.component.html
│   ├── media-folder-tree/         (NEW - folder navigation)
│   │   ├── media-folder-tree.component.ts
│   │   └── media-folder-tree.component.html
│   └── media-format-manager/      (NEW - format definitions UI)
│       ├── media-format-manager.component.ts
│       └── media-format-manager.component.html
└── index.ts
```

### 4.6 Frontend API Endpoints (api-endpoints.ts)

```typescript
// Media
media: 'media',
mediaById: 'media/${id}',
mediaByUid: 'media/by-uid/${uid}',
mediaUpload: 'media/upload',
mediaComposite: 'media/composite',
mediaI18n: 'media/${mediaId}/i18n/${language}',

// Media Containers
mediaContainers: 'media/containers',
mediaContainerById: 'media/containers/${id}',
mediaContainerGenerate: 'media/containers/${id}/generate',

// Media Formats
mediaFormats: 'media/formats',
mediaFormatById: 'media/formats/${id}',

// Media Folders
mediaFolders: 'media/folders',
mediaFolderById: 'media/folders/${id}',
mediaFolderMedia: 'media/folders/${id}/media',
mediaFolderMove: 'media/folders/${id}/move',

// CMS Delivery (public)
cmsMedia: 'cms/media/${uid}',
cmsMediaBatch: 'cms/media',
```

---

## 5. Implementation Phases (Full DAM)

### Phase 1: Database & Entity Foundation (Priority: HIGH)

**Files to create/modify:**

- `db/tenant/media/V6__media_baseline.sql` (NEW)
- `db/tenant/media/R__seed_media_formats.sql` (NEW)
- `domain/entity/Media.java` (NEW - replace MediaFile)
- `domain/entity/MediaI18n.java` (NEW)
- `domain/entity/MediaFormat.java` (NEW)
- `domain/entity/MediaFolder.java` (NEW)
- `domain/entity/MediaContainer.java` (NEW)
- `domain/entity/MediaContainerItem.java` (NEW)
- `domain/enums/StorageProvider.java` (NEW)
- `domain/enums/MediaStatus.java` (NEW)
- `domain/enums/CropMode.java` (NEW)

**Tasks:**

1. Create migration file with all tables
2. Create repeatable seed for system formats
3. Create all entity classes with BaseEntity
4. Create repository interfaces
5. Create JPA repositories
6. Register module version in CLAUDE.md

## 12. Phase 1 & Core Implementation Status

**Scope**: Database, Entities, Basic Repositories, Core Service, and Controller.

### 12.1 Changes Implemented

1.  **Database Schema ([V20\_\_media_baseline.sql](cci:7://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/resources/db/tenant/media/V20__media_baseline.sql:0:0-0:0))**

    - Full DAM structure established: `media`, `media_i18n`, `media_folders`, `media_formats`, `media_containers`, `media_container_items`.
    - Tenant-ready structure (Database-per-tenant).
    - Index limit optimization: `path` column set to `VARCHAR(768)`.

2.  **Seed Data ([R\_\_seed_media_formats.sql](cci:7://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/resources/db/tenant/media/R__seed_media_formats.sql:0:0-0:0))**

    - System formats defined: `THUMBNAIL`, `SMALL`, `MEDIUM`, `LARGE`, `ZOOM`.
    - Root folder (`/`) automatically created.

3.  **Entity Layer**

    - [Media](cci:2://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/java/com/backend/domain/entity/MediaFolder.java:26:0-85:1): Main file entity (BaseEntity).
    - [MediaI18n](cci:2://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/java/com/backend/infrastructure/persistence/repository/MediaI18nRepositoryImpl.java:13:0-68:1): Multi-language support (BaseI18nEntity).
    - [MediaFolder](cci:2://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/java/com/backend/domain/entity/MediaFolder.java:26:0-85:1): Hierarchical folder structure.
    - [MediaFormat](cci:2://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/java/com/backend/infrastructure/persistence/repository/MediaFormatJpaRepository.java:10:0-22:1): Image processing format definitions.
    - [MediaContainer](cci:2://file:///c:/Users/emreerkesikbas/Documents/AdminCraft/backend/src/main/java/com/backend/infrastructure/persistence/repository/MediaContainerJpaRepository.java:9:0-21:1) & `Item`: SAP Hybris variation pattern.

4.  **Repository Layer (Performance Optimized)**

    - **N+1 Prevention**: `@EntityGraph` utilized in `MediaContainerJpaRepository` and `MediaJpaRepository`.
    - **Pagination**: `MediaRepositoryImpl` implements efficient paginated queries for large datasets.

5.  **Service & Presentation (Core CRUD)**
    - **Service**: `MediaServiceImpl` implements transactional file operations.
    - **Controller**: `MediaController` exposes endpoints returning `MediaResponse` DTOs (not entities).
    - **DTO**: `MediaResponse` record created for standardized API output.

### 12.2 Verification Steps

1.  **Start Backend:**
    ```bash
    mvn spring-boot:run -Dspring-boot.run.profiles=dev
    ```
2.  **Check Migrations:**
    Verify success in logs: `Migrating schema to version "20 - media baseline"`.

3.  **Check Database (SQL):**

    ```sql
    USE ac_tenant_[id];
    SHOW TABLES LIKE 'media%';
    -- Expected: media, media_containers, media_container_items, media_folders, media_formats, media_i18n

    SELECT * FROM media_formats;
    -- Expected: 5 system formats (THUMBNAIL etc.)

    SELECT * FROM media_folders;
    -- Expected: 1 Root folder
    ```

### 12.3 Developer Notes

- Entities are located in `com.backend.domain.entity` package with `Media*` prefix.
- Repositories are not yet created (Phase 3).
- Do not confuse with existing `MediaFile` entity, it will be deprecated.

---

## 13. Phase 2 Implementation Status

**Scope**: Storage Adapter Pattern, Image Processing with Thumbnailator, Async Format Generation.

### 13.1 User Decisions

| Decision              | Choice               | Rationale                                                   |
| --------------------- | -------------------- | ----------------------------------------------------------- |
| Processing Mode       | **Async**            | Upload returns immediately, formats generated in background |
| Storage Structure     | **Tenant Subfolder** | `uploads/{tenantId}/media/{filename}`                       |
| Auto-Generate Formats | **3 formats**        | THUMBNAIL, SMALL, MEDIUM                                    |

### 13.2 Files Created

**Infrastructure Layer:**

- `infrastructure/storage/LocalStorageAdapter.java` - Local filesystem implementation
- `infrastructure/storage/StorageProperties.java` - `@ConfigurationProperties` binding
- `infrastructure/storage/StorageException.java` - Runtime exception
- `infrastructure/config/StorageConfig.java` - `@EnableConfigurationProperties`

**Application Layer - Services:**

- `application/service/StorageAdapter.java` - Storage interface (Moved from Infrastructure)
- `application/service/MediaStorageService.java` - Interface (store, retrieve, validate)
- `application/service/MediaStorageServiceImpl.java` - Magic bytes validation, MIME check
- `application/service/MediaProcessingService.java` - Interface (generateFormats, extractDimensions)
- `application/service/MediaProcessingServiceImpl.java` - `@Async` + Thumbnailator resize
- `application/service/MediaContainerService.java` - Interface (createForMedia, addVariant)
- `application/service/MediaContainerServiceImpl.java` - Container + item management

**Domain Layer:**

- `domain/exception/MediaExceptions.java` - Custom exceptions

### 13.3 Files Modified

- `pom.xml` - Added `net.coobird:thumbnailator:0.4.20`
- `application.yml` - Added `admincraft.storage.*` configuration
- `MediaServiceImpl.java` - Integrated new services
- `MediaFormatRepository.java` - Added `findByCodeIn(Collection<String>)`
- `MediaFormatJpaRepository.java` - Added `findByCodeIn`
- `MediaFormatRepositoryImpl.java` - Added delegation

### 13.4 Key Features

1. **Tenant Isolation**: Files stored in `uploads/{tenantId}/media/` and `uploads/{tenantId}/variants/`
2. **Magic Bytes Validation**: JPEG (FF D8 FF), PNG (89 50 4E 47), GIF, PDF signatures checked
3. **Async Processing**: `@Async("taskExecutor")` with MDC logging for correlation
4. **Format Generation**: Thumbnailator resizes to THUMBNAIL (100x100), SMALL (300x300), MEDIUM (600x600)

### 13.5 Configuration (application.yml)

```yaml
admincraft:
  storage:
    provider: local
    base-path: uploads
    max-file-size: 10485760 # 10MB
    processing:
      async-enabled: true
      auto-generate-formats: THUMBNAIL,SMALL,MEDIUM
```

### 13.6 Upload Workflow

```
1. MediaController.upload(file)
2. MediaStorageService.validate(file)     → Magic bytes + MIME + size
3. MediaStorageService.store(file)        → uploads/{tenantId}/media/{uuid}.ext
4. MediaRepository.save(media)            → Status: PROCESSING
5. MediaContainerService.createForMedia() → Create container
6. MediaProcessingService.generateFormats() → @Async
   └── For each format (THUMBNAIL, SMALL, MEDIUM):
       ├── Thumbnailator.resize()
       ├── StorageAdapter.store() → uploads/{tenantId}/variants/{uuid}_{FORMAT}.ext
       ├── MediaRepository.save(variant)
       └── MediaContainerService.addVariant()
7. Media.status = ACTIVE
```

### 13.7 Developer Notes

- `StorageAdapter` is interface-based for future S3/Azure support (Located in Application Layer)
- `TenantContext.getTenantId()` used for path isolation
- Format generation failures don't block upload (individual format errors logged)
- `MediaContainer` groups master + variants (SAP Hybris pattern)
- **Refactoring Note**: I/O operations in `MediaServiceImpl` and `MediaProcessingServiceImpl` moved outside `@Transactional` boundaries to prevent connection pool exhaustion.
- **Refactoring Note**: `StorageAdapter` moved to Application layer to adhere to Clean Architecture Dependency Rule.

---

### Phase 2: Storage & Processing Services ✅ COMPLETED

> See **Section 13** for implementation details.

### Phase 3: Core Backend Services ✅ COMPLETED

> Build successful. 6 new files created.

**Files Created:**

```
backend/src/main/java/com/backend/application/service/
├── MediaI18nService.java        ✅
├── MediaI18nServiceImpl.java    ✅
├── MediaFolderService.java      ✅
├── MediaFolderServiceImpl.java  ✅
├── MediaFormatService.java      ✅
└── MediaFormatServiceImpl.java  ✅
```

**Features Implemented:**

| Service            | Methods                                                                                                                                       | Key Features                                                                                    |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| MediaI18nService   | `upsert`, `get`, `getByMediaId`, `delete`, `deleteAllByMediaId`, `exists`                                                                     | Find-or-create pattern, multi-language support                                                  |
| MediaFolderService | `create`, `update`, `delete`, `move`, `findById/ByUid/ByCode/ByPath`, `findAll`, `findRootFolders`, `findChildren`, `isEmpty`, `generatePath` | Path auto-calculation, safe deletion (empty folders only), circular reference detection on move |
| MediaFormatService | `create`, `update`, `delete`, `findById/ByUid/ByCode`, `findAll`, `findSystemFormats`, `findCustomFormats`, `isSystemFormat`                  | System format protection, dimension validation (1-10000), quality validation (1-100)            |

**Implementation Highlights:**

1. **Transaction Management** - Write operations use `@Transactional`, read operations use `@Transactional(readOnly=true)`
2. **Constructor Injection** - Using `@RequiredArgsConstructor` with Lombok
3. **Logging** - Debug level for operation start, Info level for results
4. **Validation** - Business rule validation at service layer

### Phase 4: Backend Controllers & DTOs (Priority: HIGH)

**Files to create:**

- `presentation/controller/MediaController.java` (DONE - Phase 1)
- `presentation/controller/MediaContainerController.java` (NEW)
- `presentation/controller/MediaFolderController.java` (NEW)
- `presentation/controller/MediaFormatController.java` (NEW)
- `presentation/controller/CmsMediaDeliveryController.java` (NEW)
- `presentation/dto/request/MediaUploadRequest.java`
- `presentation/dto/request/MediaUpdateRequest.java`
- `presentation/dto/request/MediaI18nRequest.java`
- `presentation/dto/request/MediaContainerRequest.java`
- `presentation/dto/request/MediaFolderRequest.java`
- `presentation/dto/response/MediaResponse.java` (DONE - Phase 1)
- `presentation/dto/response/MediaDetailResponse.java`
- `presentation/dto/response/MediaContainerResponse.java`
- `presentation/dto/response/MediaFolderResponse.java`
- `presentation/dto/response/MediaFormatResponse.java`

**Tasks:**

1. Create all request/response DTOs
2. Implement MediaController with multipart upload
3. Implement container, folder, format controllers
4. Implement CMS public delivery endpoint
5. Add to SecurityConfig (TenantFilter whitelist if needed)
6. Add OpenAPI documentation

### Phase 5: Frontend Services & Types (Priority: HIGH)

**Files to create/modify:**

- `media/services/media.service.ts` (REFACTOR)
- `media/services/media.store.ts` (NEW)
- `media/services/media-container.service.ts` (NEW)
- `media/services/media-folder.service.ts` (NEW)
- `media/services/media-format.service.ts` (NEW)
- `media/models/media.types.ts` (REFACTOR)
- `core/api/api-endpoints.ts` (ADD endpoints)

**Tasks:**

1. Define all TypeScript interfaces
2. Create media service extending CrudHttpService
3. Create media store extending CrudStore
4. Create specialized services
5. Add API endpoints to configuration

### Phase 6: Frontend Components (Priority: HIGH)

**Files to create:**

- `media/components/media-list/` (NEW)
- `media/components/media-upload-dialog/` (NEW)
- `media/components/media-detail-dialog/` (NEW)
- `media/components/media-gallery/` (NEW)
- `media/components/media-folder-tree/` (NEW)

**Tasks:**

1. Create MediaListComponent with grid/list toggle
2. Create MediaUploadDialog with drag & drop
3. Create MediaDetailDialog with i18n tabs
4. Create MediaGallery visual grid view
5. Create MediaFolderTree navigation component
6. Add to navigation (requires media module)

### Phase 7: Integration & Polish (Priority: MEDIUM)

**Files to create:**

- `media/components/media-picker-dialog/` (NEW)
- `media/components/media-format-manager/` (NEW)

**Tasks:**

1. Create MediaPickerDialog for other modules
2. Create MediaFormatManager UI
3. Integrate with Component Library (image fields)
4. Add bulk upload support
5. Add search and filtering
6. Add pagination

### Phase 8: Advanced Features (Priority: LOW - Future)

**Tasks:**

1. S3/Azure storage adapter
2. CDN integration
3. Video processing
4. EXIF metadata extraction
5. AI-powered alt text generation
6. Image optimization (WebP, lazy loading)

---

## 6. Design Decisions (CONFIRMED)

| Decision          | Choice            | Rationale                                                     |
| ----------------- | ----------------- | ------------------------------------------------------------- |
| **Entity Design** | Yeniden Tasarla   | BaseEntity + MediaI18n pattern, Component Library ile tutarlı |
| **Tenant Scope**  | Tenant-Specific   | Her tenant izole, database-per-tenant pattern                 |
| **Storage**       | Local File System | Başlangıç için basit, S3/Azure sonra eklenebilir              |
| **Features**      | Full DAM          | Core + Thumbnails + MediaContainer + formats                  |

---

## 7. Risk Assessment

| Risk                         | Impact | Mitigation                                         |
| ---------------------------- | ------ | -------------------------------------------------- |
| MediaFile entity refactoring | HIGH   | Create new `Media` entity, deprecate old MediaFile |
| Large file uploads           | MEDIUM | Streaming upload, file size limits (10MB default)  |
| Storage costs                | MEDIUM | Compression, quotas per tenant, cleanup jobs       |
| Migration conflicts          | LOW    | Verify V6 is available before creating             |
| Image processing memory      | MEDIUM | Use streaming resize, async processing             |
| Folder hierarchy complexity  | LOW    | Limit depth to 5 levels, recursive CTE queries     |

---

## 8. Critical Files to Modify/Create

### Backend (Priority Order)

1. `db/tenant/media/V6__media_baseline.sql` - All tables
2. `db/tenant/media/R__seed_media_formats.sql` - System formats
3. `domain/entity/Media.java` - Main entity
4. `domain/entity/MediaI18n.java` - i18n entity
5. `domain/entity/MediaContainer.java` - Container entity
6. `domain/entity/MediaFolder.java` - Folder entity
7. `domain/entity/MediaFormat.java` - Format definitions
8. `infrastructure/storage/LocalStorageAdapter.java` - File storage
9. `application/service/MediaProcessingService.java` - Resize/format
10. `presentation/controller/MediaController.java` - REST API

### Frontend (Priority Order)

1. `media/models/media.types.ts` - TypeScript interfaces
2. `media/services/media.service.ts` - HTTP service
3. `media/components/media-list/` - Main list view
4. `media/components/media-upload-dialog/` - Upload UI
5. `media/components/media-detail-dialog/` - Edit/i18n UI
6. `core/api/api-endpoints.ts` - Endpoint config

---

## 9. Estimated Effort

| Phase                         | Effort          | Dependencies |
| ----------------------------- | --------------- | ------------ |
| Phase 1: Database & Entities  | 4-6 hours       | None         |
| Phase 2: Storage & Processing | 4-6 hours       | Phase 1      |
| Phase 3: Core Services        | 6-8 hours       | Phase 1, 2   |
| Phase 4: Controllers & DTOs   | 4-6 hours       | Phase 3      |
| Phase 5: Frontend Services    | 2-4 hours       | Phase 4      |
| Phase 6: Frontend Components  | 8-10 hours      | Phase 5      |
| Phase 7: Integration          | 4-6 hours       | Phase 6      |
| **Total**                     | **32-46 hours** |              |

---

## 10. Success Criteria

- [ ] All 6 tables created and migrated
- [ ] File upload working with local storage
- [ ] Automatic format generation (THUMBNAIL, SMALL, MEDIUM, LARGE, ZOOM)
- [ ] MediaContainer groups master + variants
- [ ] i18n metadata (alt text, title, description) per language
- [ ] Folder hierarchy with tree navigation
- [ ] Frontend list/upload/detail dialogs
- [ ] CMS public delivery endpoint working
- [ ] Integration with Component Library image fields

---

## 11. Analiz Notları

> [!NOTE]
> Bu bölüm, SAP Hybris media modülü analizi sonrası eklenen notları içerir.

### 11.1 Hybris ile Uyum Durumu

| Özellik                    | Hybris | AdminCraft | Uyum           |
| -------------------------- | ------ | ---------- | -------------- |
| MediaModel → Media entity  | ✅     | ✅         | 🟢 Tam uyum    |
| MediaContainer grouping    | ✅     | ✅         | 🟢 Tam uyum    |
| Format generation pipeline | ✅     | ✅         | 🟢 Tam uyum    |
| Folder hierarchy           | ✅     | ✅         | 🟢 Tam uyum    |
| i18n metadata pattern      | ✅     | ✅         | 🟢 Tam uyum    |
| Storage adapter pattern    | ✅     | ✅         | 🟢 Tam uyum    |
| Catalog version linkage    | ✅     | ❌ Skip    | 🟡 Doğru karar |

### 11.2 Dikkat Edilmesi Gerekenler

| Konu                              | Öneri                                                                        |
| --------------------------------- | ---------------------------------------------------------------------------- |
| **MediaFile → Media refactoring** | Mevcut `MediaFile` entity deprecate edilip yeni `Media` entity oluşturulmalı |
| **Hardcoded languages**           | `altTextTr`, `altTextEn` yerine `media_i18n` tablosu kullanılmalı            |
| **BaseEntity extension**          | Tüm entity'ler proje standardına uygun olmalı                                |
| **File validation**               | OWASP file upload security kontrolleri eklenmeli                             |

### 11.3 Atlanan Özellikler (Doğrulandı)

- **Catalog Version Linkage**: AdminCraft'ta product catalog yok ✅
- **Complex DAM Workflows**: CMS scope dışı ✅
- **Asset Versioning**: YAGNI prensibi ✅

---

## 14. Phase 4 Implementation Status & Refactoring

> [!NOTE]
> Phase 4 completed on 2026-01-01. Includes controllers, DTOs, and extensive code review improvements.

### 12.1 Request DTOs

| DTO                        | Validation                                     | Usage                     |
| -------------------------- | ---------------------------------------------- | ------------------------- |
| `MediaFolderCreateRequest` | `@NotBlank name`, `@Size(max=100)`             | Create folder             |
| `MediaFolderUpdateRequest` | `@NotBlank name`, `@Size(max=100)`             | Update folder name        |
| `MediaFolderMoveRequest`   | nullable `parentId`                            | Move folder (null = root) |
| `MediaFormatCreateRequest` | code, name, width, height, quality, scale mode | Custom format             |
| `MediaFormatUpdateRequest` | Same as create (all optional except ID)        | Update format             |
| `MediaI18nRequest`         | altText, title, description `@Size` limits     | Localized metadata        |
| `MediaUpdateRequest`       | folderId, isPublic, tags list                  | Update media              |

### 12.2 Response DTOs

| DTO                      | Pattern                                                | Notes                          |
| ------------------------ | ------------------------------------------------------ | ------------------------------ |
| `MediaFolderResponse`    | `from(MediaFolder)`                                    | Includes depth, children count |
| `MediaFormatResponse`    | `from(MediaFormat)`                                    | Includes isSystem flag         |
| `MediaI18nResponse`      | `from(MediaI18n)`                                      | Language code as String        |
| `MediaContainerResponse` | `from(MediaContainer)` + nested `MediaVariantResponse` | Master + variants              |
| `MediaDetailResponse`    | `from(Media, Container, List<I18n>)`                   | Composite detail               |

### 12.3 API Endpoints

**MediaFolderController** (`/media/folders`)

```
GET    /           → List all      GET    /{id}       → By ID
GET    /root       → Root folders  PUT    /{id}       → Update
GET    /{id}/children               PUT    /{id}/move  → Move
GET    /{id}/media                  POST   /           → Create
                                    DELETE /{id}       → Delete
```

**MediaFormatController** (`/media/formats`)

```
GET    /           → List all      PUT    /{id}       → Update custom
GET    /system     → System only   POST   /           → Create custom
GET    /custom     → Custom only   DELETE /{id}       → Delete custom
GET    /{id}       → By ID
```

> [!IMPORTANT]
> System formats (THUMBNAIL, SMALL, etc.) are protected from update/delete.

**CmsMediaDeliveryController** (`/cms/media`) - **PUBLIC** (Rate Limited: 100 req/min per tenant)

```
GET    /{uid}                → Public media
GET    /{uid}/format/{code}  → Specific variant
GET    /batch?uids=a,b,c     → Batch (max 20)
```

**MediaController** (`/media`) - Extended

```
PUT    /{id}               → Update metadata
GET    /{id}/i18n/{lang}   → Get translation
PUT    /{id}/i18n/{lang}   → Upsert translation
DELETE /{id}/i18n/{lang}   → Delete translation
GET    /{id}/detail        → Full detail view
GET    ?page=0&size=20     → Paginated list
```

### 12.4 Security

| Controller                   | Auth           | Tenant                             |
| ---------------------------- | -------------- | ---------------------------------- |
| `MediaFolderController`      | `TENANT_ADMIN` | Required                           |
| `MediaFormatController`      | `TENANT_ADMIN` | Required                           |
| `MediaController`            | `TENANT_ADMIN` | Required                           |
| `CmsMediaDeliveryController` | **None**       | Required via header (Rate Limited) |

> CMS delivery uses `X-Tenant-ID` or `X-Tenant-Subdomain` header for tenant resolution.

### 12.5 Service Methods Added

```java
// MediaService
Media updateMetadata(Long id, Long folderId, Boolean isPublic, List<String> tags)
Page<Media> findAll(Pageable pageable)
Media moveToFolder(Long mediaId, Long folderId)

// MediaContainerService
Optional<MediaContainer> findByMasterMediaId(Long masterMediaId) // alias
```

### 14.6 Code Review Improvements (Refactoring)

**1. Performance Optimization (P0)**

- **Issue**: N+1 Query problem in `CmsMediaDeliveryController.getMediaBatch`.
- **Fix**: Implemented `findByUidIn` in repository and service to fetch all media in a single query.

**2. Security Hardening (P0, P2)**

- **User Context**: Removed hardcoded fallback users. Now explicitly requires authenticated `SecurityContext` or throws error.
- **Rate Limiting**: Implemented `Guava RateLimiter` on `/api/cms/media/**` endpoints.
  - Limit: 100 requests per minute per tenant.
  - Storage: In-memory `ConcurrentHashMap<TenantId, RateLimiter>`.

**3. API Documentation (P2)**

- **OpenAPI/Swagger**: Added `@Tag`, `@Operation`, and `@Parameter` annotations to all controllers.
- **Dependency**: Included `springdoc-openapi-starter-webmvc-ui`.

**4. Observability (P1)**

- **MDC support**: Added `tenantId` and `correlationId` to MDC context in CMS public endpoints for better log tracing.

---

---
