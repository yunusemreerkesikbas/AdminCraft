import { Routes } from '@angular/router';
import { AuthSetPasswordComponent } from 'app/modules/auth/set-password/set-password.component';

export default [
    {
        path: '',
        component: AuthSetPasswordComponent,
    },
] as Routes;
